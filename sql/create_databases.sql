-- ============================================================
-- ABC Hub Database Creation
-- Run this while connected to the default postgres database.
-- Intended for a fresh setup.
-- Skip this script if the databases already exist; it is not safe to rerun.
-- ============================================================

CREATE DATABASE abc_hub_operational;
CREATE DATABASE abc_hub_analytics;
